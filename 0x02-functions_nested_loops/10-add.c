#include "main.h"
/**
 * add - write a function that adds two integers and returns the result
 * Return: always 0
 * @a: first integer
 * @b: second integer
 *
*/
int add(int a, int b)
{
	int c = a + b;

	return (c);
}
