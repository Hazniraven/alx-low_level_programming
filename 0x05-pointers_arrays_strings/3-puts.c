#include "main.h"
#include <stdio.h>
/**
 * _puts - write a function that prints a string, followed by
 * a new line to stdout
 * @str: string
 * Return: none
 */
void _puts(char *str)
{
	puts(str);
}
